- hosts: localhost
  connection: local
  become: true

  vars:
    app_name: go-sample
    app_user: goapp
    app_group: goapp
    app_root: /opt/go-sample-app
    app_project_dir: /opt/go-sample-app/go-app
    app_bin_dir: /opt/go-sample-app/bin
    app_bin_path: /opt/go-sample-app/bin/go-sample
    app_port: "8080"

  tasks:
    - name: Install packages for Go sample app
      package:
        name:
          - golang-go
          - git
          - curl
          - ca-certificates
          - sqlite3
        state: present

    - name: Create group for app user
      group:
        name: "{{ app_group }}"
        state: present

    - name: Ensure docker group exists
      group:
        name: docker
        state: present

    - name: Create app user and add docker group
      user:
        name: "{{ app_user }}"
        group: "{{ app_group }}"
        groups: docker
        append: true
        create_home: false
        shell: /usr/sbin/nologin
        system: true

    - name: Create app directories
      file:
        path: "{{ item }}"
        state: directory
        owner: "{{ app_user }}"
        group: "{{ app_group }}"
        mode: "0755"
      loop:
        - "{{ app_root }}"
        - "{{ app_project_dir }}"
        - "{{ app_bin_dir }}"

    - name: Deploy standalone go-app project
      copy:
        src: "./go-app/"
        dest: "{{ app_project_dir }}/"
        owner: "{{ app_user }}"
        group: "{{ app_group }}"
        mode: "0644"
        directory_mode: "0755"
      register: go_project_result

    - name: Check app binary
      stat:
        path: "{{ app_bin_path }}"
      register: app_binary

    - name: Build Go sample app
      command: /usr/bin/env bash -lc 'cd {{ app_project_dir }} && go build -o {{ app_bin_path }} .'
      when: go_project_result.changed or not app_binary.stat.exists
      notify: Restart go sample service

    - name: Set binary ownership
      file:
        path: "{{ app_bin_path }}"
        owner: root
        group: root
        mode: "0755"

    - name: Install systemd unit
      copy:
        dest: /etc/systemd/system/go-sample.service
        mode: "0644"
        content: |
          [Unit]
          Description=Go sample web application
          After=network-online.target docker.service
          Wants=network-online.target

          [Service]
          Type=simple
          User={{ app_user }}
          Group={{ app_group }}
          WorkingDirectory={{ app_project_dir }}
          ExecStart={{ app_bin_path }}
          Environment=APP_PORT={{ app_port }}
          Restart=on-failure
          RestartSec=3

          [Install]
          WantedBy=multi-user.target
      notify:
        - Reload systemd
        - Restart go sample service

    - name: Enable and start go sample service
      systemd:
        name: go-sample
        enabled: true
        state: started

  handlers:
    - name: Reload systemd
      systemd:
        daemon_reload: true

    - name: Restart go sample service
      systemd:
        name: go-sample
        state: restarted
